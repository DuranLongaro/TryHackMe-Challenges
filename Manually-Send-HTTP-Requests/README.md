### Find The Flag!

Used `telnet` for an unencrypted communication to the `HTTP port 80`

In order to find the "flag" I needed to use the `GET` feature and retrieve the contents of `flag.thm`

![Terminal](screenshots/Manual_Request)

